import React from "react";
import { Chart, LineChart } from "react-charts";
import Markdown from "react-markdown";

const Dashboard = () => {
  return (
    <div>
      {/* Dashboard components */}
    </div>
  );
};

export default Dashboard;